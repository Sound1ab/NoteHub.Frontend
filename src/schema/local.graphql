type Query {
  currentRepoName: String
  currentFileName: String
  currentPath: String
  currentTheme: String
  isEdit: Boolean!
  cursorPosition: Position!
  jwt: String
}

type Position {
  ch: Int!
  line: Int!
}
