type Query {
  currentRepoName: String
  currentPath: String
  currentTheme: String
  isEdit: Boolean!
  cursorPosition: Position!
  jwt: String
  accentColor: String
}

type Position {
  ch: Int!
  line: Int!
}
