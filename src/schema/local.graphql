type Query {
  currentRepoName: String
  currentFileName: String
  currentTheme: String
  isEdit: Boolean!
  isNewFileOpen: Boolean!
  cursorPosition: Position!
}

type Position {
  ch: Int!
  line: Int!
}
